/**
 * MediaItem interface represents a media item such as a movie or TV show.
 */
export interface MediaItem {
  id: number;
  title: string;
  still: string;
  year: string;
  rating: string;
  type: string;
}
